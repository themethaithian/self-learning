// Package domain models the learning bounded context: LessonProgress and the
// ChunkState lifecycle it moves through, plus the pure Gate function that
// decides which lessons in a chapter are currently unlocked.
package domain
